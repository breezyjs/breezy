/** Update an existing pet by Id */
export function updatePet(factory: any) {
}

/** Add a new pet to the store */
export function addPet(factory: any) {
}

/** Multiple status values can be provided with comma separated strings */
export function findPetsByStatus(factory: any) {
}

/** Multiple tags can be provided with comma separated strings. Use tag1, tag2, tag3 for testing. */
export function findPetsByTags(factory: any) {
}

/** Returns a single pet */
export function getPetById(factory: any) {
}

export function updatePetWithForm(factory: any) {
}

/** delete a pet */
export function deletePet(factory: any) {
}

export function uploadFile(factory: any) {
}

/** Returns a map of status codes to quantities */
export function getInventory(factory: any) {
}

/** Place a new order in the store */
export function placeOrder(factory: any) {
}

/** For valid response try integer IDs with value <= 5 or > 10. Other values will generate exceptions. */
export function getOrderById(factory: any) {
}

/** For valid response try integer IDs with value < 1000. Anything above 1000 or nonintegers will generate API errors */
export function deleteOrder(factory: any) {
}

/** This can only be done by the logged in user. */
export function createUser(factory: any) {
}

/** Creates list of users with given input array */
export function createUsersWithListInput(factory: any) {
}

export function loginUser(factory: any) {
}

export function logoutUser(factory: any) {
}

export function getUserByName(factory: any) {
}

/** This can only be done by the logged in user. */
export function updateUser(factory: any) {
}

/** This can only be done by the logged in user. */
export function deleteUser(factory: any) {
}
